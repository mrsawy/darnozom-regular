# Medusa v2 environment configuration
# 9010, not 9000 — 9000 is usually already taken locally.
PORT=9010
MEDUSA_ADMIN_ONBOARDING_TYPE=default
MEDUSA_DATABASE_URL=postgresql://darnozom:darnozom@localhost:5434/medusa
MEDUSA_REDIS_URL=redis://localhost:6381
MEDUSA_JWT_SECRET=
MEDUSA_COOKIE_SECRET=
MEDUSA_ADMIN_CORS=http://localhost:9010
# Vite may bump past 5173 when that port is busy — allow common fallbacks.
MEDUSA_STORE_CORS=http://localhost:5173,http://localhost:5174,http://localhost:5175,http://localhost:5176,http://localhost:5550,http://localhost:3000
AUTH_MFA_ENCRYPTION_KEY=

# Local file provider (product image uploads). Both optional locally —
# defaults to <cwd>/static and http://localhost:9010/static, which is fine
# for dev. In production deploy.sh sets MEDUSA_UPLOAD_DIR outside the
# redeployed app dir, and MEDUSA_UPLOAD_BACKEND_URL to
# https://ecommerce.<domain>/static (served by nginx.medusa.conf /static/).
# MEDUSA_UPLOAD_DIR=
# MEDUSA_UPLOAD_BACKEND_URL=

# Product type id (Admin > Settings > Product Types, "ptyp_...") used by:
# - POST /admin/books (Create Book Admin form) — required to create books
# - auto-add-book-editions subscriber — safety net when using stock Create Product
# Unset by default (Create Book returns 400; subscriber is a no-op).
# MEDUSA_BOOK_PRODUCT_TYPE_ID=ptyp_...

# Better Auth bridge secret (shared with apps/api via root .env)
BETTER_AUTH_BRIDGE_SECRET=

# Express API base URL — used by order.placed to push Socket.IO admin alerts
# and by the Vodafone Cash / InstaPay "Mark as paid" sync
# DARNOZOM_API_URL=http://127.0.0.1:8087
# Browser-facing API address for the admin order bell socket (/api/socket.io).
# Dev default: http://localhost:8087. Production: https://darnozom.com
# DARNOZOM_PUBLIC_API_URL=http://localhost:8087

# Private file store shared with apps/api (same values as the root .env):
# Medusa saves digital-book files here, the API serves them to buyers.
OBJECT_STORAGE_BACKEND=local
PRIVATE_OBJECT_DIR=

# Payment gateways (optional — needed if testing direct Medusa payment sessions)
PAYMOB_API_KEY=
PAYMOB_INTEGRATION_ID=
PAYMOB_HMAC_SECRET=
PAYMOB_IFRAME_ID=
PAYMOB_WALLET_INTEGRATION_ID=

PAYPAL_CLIENT_ID=
PAYPAL_CLIENT_SECRET=
PAYPAL_ENVIRONMENT=sandbox

LEMONSQUEEZY_API_KEY=
LEMONSQUEEZY_STORE_ID=
LEMONSQUEEZY_WEBHOOK_SECRET=

# Resend (Medusa Notification Module — order confirmation, invites, password reset).
# Same credentials as apps/api. Leave unset locally to skip email provider registration.
# RESEND_API_KEY=
# RESEND_FROM_EMAIL=Darnozom Consulting <noreply@darnozom.com>

