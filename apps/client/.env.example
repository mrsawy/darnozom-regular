# Clerk auth (optional — site still loads without it; admin/auth routes show a notice)
VITE_CLERK_PUBLISHABLE_KEY=

# When `npm run dev` proxies /api requests, point at your API server
VITE_API_PROXY_TARGET=http://localhost:8080
