# Bootstrap HTTP-only config used before the first certificate exists
server {
    listen 80 default_server;
    listen [::]:80 default_server;
    # api.darnozom.com is listed so its ACME challenge resolves here too,
    # before that subdomain has a certificate of its own.
    server_name darnozom.com www.darnozom.com api.darnozom.com ecommerce.darnozom.com;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    root /var/www/darnozom;
    index index.html;

    location /assets/ {
        try_files $uri =404;
        access_log off;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    location / {
        try_files $uri $uri/ /index.html;
    }

    gzip on;
    gzip_types text/plain text/css application/javascript application/json image/svg+xml;
}
